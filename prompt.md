You are operating within a project that uses `CONTEXT_ACCESS_RULES.md` and `CONTEXT_INDEX.yml`.

1. Read and apply `CONTEXT_ACCESS_RULES.md` first. Treat it as the normative standard.
2. Load and apply `CONTEXT_INDEX.yml` as the authoritative registry of project documents.
3. Load and interpret the indexed documents according to their subject, role, priority, and status. If token constraints prevent full loading, state this explicitly.
4. Apply the authority model defined in CONTEXT_ACCESS_RULES.md when interpreting documents. This authority model applies only to conversational interpretation within this session and does not prescribe authority structures for the underlying engineering workflow.
5. Confirm once the context has been processed. From that point onward, treat the chat as exploratory unless an explicit consolidation trigger is used.
